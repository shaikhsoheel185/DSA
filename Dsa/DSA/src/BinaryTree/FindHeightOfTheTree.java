package BinaryTree;



/*

Given a binary tree, the task is to find the height of the tree. Height of the empty tree is 0.

*/



public class FindHeightOfTheTree {

    /*

    public int maxDepth ( TreeNode root ) {

        if ( root == null) return 0;

      int Left=  maxDepth( root.left);          // Time Complexity - O(n)
      int Right= maxDepth( root.right);

      return Math.max(Left,Right)+1;

    }

    */
}
